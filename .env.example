# RTL-HAOS Configuration
# Copy this file to .env and edit values for your environment
# Uncomment variables below to override default values

# --- MQTT SETTINGS ---
MQTT_HOST=192.168.1.123
MQTT_USER=mqtt_user
MQTT_PASS=mqtt_password
# MQTT_PORT=1883
# MQTT_KEEPALIVE=60

#BRIDGE_ID=42

# --- PUBLISHING SETTINGS ---
# expire_after in seconds for sensor entities in Home Assistant
# RTL_EXPIRE_AFTER=600

# Time in seconds to buffer data before sending
# 0 = send immediately (real-time / no averaging)
# >0 = average numeric values, use last value for non-numeric
# RTL_THROTTLE_INTERVAL=30

# If true, print raw rtl_433 JSON to stdout for debugging
# DEBUG_RAW_JSON=false

# Set to true to force new unique_ids (adds _v2 suffix)
# FORCE_NEW_IDS=false

# --- RTL-SDR CONFIGURATION ---
# List of RTL-SDR radio configurations (JSON format)
# Leave empty to auto-detect a single RTL-SDR
# RTL_CONFIG='[{"name": "Weather Radio", "id": "101", "freq": "433.92M", "rate": "250k"}, {"name": "Utility Radio", "id": "102", "freq": "912.6M", "rate": "2400k"}]'

# Example: Hop between 433.92MHz and 915MHz
# RTL_CONFIG='[{"name": "Hopping_Radio", "freq": "433.92M, 915M", "rate": "1000k"}]'

# --- DEVICE FILTERING ---
# Device patterns to block (JSON array format)
# DEVICE_BLACKLIST='["SimpliSafe*", "EezTire*"]'

# Device patterns to allow (if non-empty, only these patterns are allowed)
# Please note you can filter on the Device Model, as shown, or the Device ID
# DEVICE_WHITELIST='["Acurite-5n1*", "AmbientWeather*"]'

# --- DATA PROCESSING ---
# Keys to skip when publishing sensor data (JSON array format)
# SKIP_KEYS='["time", "protocol", "mod", "id"]'

# Sensors shown in main panel vs diagnostics (JSON array format)
# MAIN_SENSORS='["temperature", "temperature_C", "temperature_F", "humidity", "pressure_hpa", "wind_avg_km_h", "rain_mm"]'

# --- RADIO DEFAULTS (standalone-only) ---
# Defaults used when a per-radio value isn't set in RTL_CONFIG
# RTL_DEFAULT_FREQ="433.92M"
# RTL_DEFAULT_RATE="250k"
# RTL_DEFAULT_HOP_INTERVAL=60

# --- UI / LOGGING ---
# If true, show "Last: HH:MM:SS" on the bridge status entity instead of "Online"
# RTL_SHOW_TIMESTAMPS=false

# If true, log every MQTT publish (very noisy)
# VERBOSE_TRANSMISSIONS=false

# --- BATTERY LOW (battery_ok) ---
# Seconds battery_ok must remain OK before clearing a low alert (0 clears immediately)
# BATTERY_OK_CLEAR_AFTER=300
